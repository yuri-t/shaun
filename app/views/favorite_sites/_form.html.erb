<%= form_for(@favorite_sites) do |f| %>
  <% if @favorite_sites.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_sites.errors.count, "error") %> prohibited this favorite_sites from being saved:</h2>

      <ul>
      <% @favorite_sites.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
